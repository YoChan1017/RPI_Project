# 가상환경 생성
python3 -m venv myenv

# 가상환경 활성화
source myenv/bin/activate

# Flask 설치
pip install flask

# 가상환경 비활성화 (필요시)
# deactivate

# 가상환경 활성화 후 파일 실행
# sudo python3 server.py


# 가상환경 라이브러리
# (myenv) pip install spidev
# (myenv) pip install RPi.GPIO
# (myenv) pip install mysql-connector-python
